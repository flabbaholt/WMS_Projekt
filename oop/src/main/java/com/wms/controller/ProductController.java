package com.wms.controller;

public class ProductController {


    /*Add-Button wurde gedrückt DAS IST NUR PSEUDOCODE
    *String[] attributes = get Data();
    *Product a = new Product(attributes);
    *try {
        a.saveData()

    *}catch (DB.exception);
    */
    
}
